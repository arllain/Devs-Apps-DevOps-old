pipeline {
    agent any

    tools {
        maven 'maven'
        jdk 'jdk'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Run Maven on a Unix agent.
                sh "mvn -f Calculadora/backend/pom.xml -Dmaven.test.failure.ignore=true clean package"
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
    }
}